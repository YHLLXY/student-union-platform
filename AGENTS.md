# AGENTS.md — AI 代理工作守则

> 本文件是 AI 代理（ZCode / Claude / CodeBuddy 等）在本仓库工作时的行为守则。代理在开始任务前应阅读并遵守；新增守则直接在下方追加小节。

## 守则 1：动效类任务必须先查本地动效库

**触发范围**：凡是任务内容涉及动效/动画/过渡/转场/加载动画/微交互/视觉特效——包括但不限于滚动动画、页面切换、粒子与背景特效、文字动效、数字滚动、骨架屏、拖拽反馈、悬停效果——一律按以下固定顺序执行，不得跳步：

1. **先复用项目内已有动效基座**（不重复造轮子）：
   - 引擎：motion（`LazyMotion + domAnimation` 模式，见现有页面用法）；
   - 封装组件：`FadeIn` / `StaggerGroup` / `CountUpNumber`；
   - 动效令牌：`themeColors` / `base.css` 中的动效变量与错峰入场约定。
2. **查本地动效库**（仓库根的上一级）：`E:\homework\开发\特效存储`（相对仓库即 `../特效存储`）：
   - 先读其中的 `INDEX.md` 定位资源，按需映射：CSS 动效→`01`、JS 引擎→`02`、滚动与转场→`03`、Lottie/SVG→`04`、粒子背景→`05`、文字→`06`、加载器与微交互→`07`、缓动曲线→`08` 与 `12-自建速查/缓动曲线速查.md`、WebGL→`09`、React 组件成品→`10`；
   - 库内有合适方案 → 以它为蓝本实现（可抄实现思路或组件源码），本地没有才进入下一步。
3. **外部寻找**：本地库无合适方案时，再去 GitHub 等渠道搜索开源实现；参考站点清单见 `特效存储/12-自建速查/灵感站与在线工具清单.md`。只采用宽松开源许可（MIT / ISC / Apache-2.0 / BSD 等）的代码，**严禁引入付费或授权受限素材**。
4. **优质资源沉淀入库**（找到值得复用的开源库时顺手做）：在 `特效存储/repos.tsv` 追加一行 `分类<TAB>名字<TAB>owner/repo<TAB>超时秒`（TAB 分隔，参考文件内已有行）→ 执行 `bash clone.sh`（幂等）→ `node gen-index.mjs` 重建索引。下次同类任务即可直接命中本地库。
5. **落地约束**：
   - 采用第三方实现时保留其 LICENSE/版权声明，遵守其许可条款；
   - 只动 `transform` / `opacity`，尊重 `prefers-reduced-motion`，时长遵循 `12-自建速查/动效时长与节奏参考.md` 的基准；
   - 视觉上对齐项目设计令牌（不得硬编码色值/时长，接入 `token` 与动效基座变量）。
