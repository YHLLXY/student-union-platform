# 论坛权限与可见性改进 — 实施计划

**Goal:** 发帖权限控制 + 删帖 + 部门隔离 + 协同部门

**Architecture:** 数据库加 collaborating_departments 列 → forumService 改查询/新增删帖和协同函数 → PostForm 加部门多选 → PostList 隐藏发帖按钮 → PostDetail 加删除和协同部门管理

**Tech Stack:** React 19 + TypeScript + Ant Design + Supabase + CSS Modules

---

### Task 1: 数据库

- [ ] SQL: `ALTER TABLE forum_posts ADD COLUMN collaborating_departments TEXT[] DEFAULT '{}';`

### Task 2: forumService.ts

- [ ] ForumPost 接口新增 `collaborating_departments: string[]`
- [ ] createPost 新增 `collaborating_departments` 参数
- [ ] fetchPosts 修改查询条件：`department = userDept OR collaborating_departments @> ARRAY[userDept]`
- [ ] 新增 `deletePost(id)` — 级联删除回复
- [ ] 新增 `updateCollaboratingDepts(postId, depts)`

### Task 3: PostForm.tsx

- [ ] presidium+ 时显示「协同部门」多选 Select
- [ ] 提交时传入 collaborating_departments

### Task 4: PostList.tsx

- [ ] volunteer 隐藏发帖按钮

### Task 5: PostDetail.tsx

- [ ] 显示发布者部门 Tag
- [ ] 删除按钮（权限判断）
- [ ] 追加协同部门（president/teacher）
