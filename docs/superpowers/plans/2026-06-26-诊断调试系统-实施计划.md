# 诊断调试系统 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建立轻量结构化诊断标签系统，实现模块级错误精准定位和 API 调用追踪。

**Architecture:** 新建独立 `src/diagnostics/` 基础设施层（5 个文件），增强现有 ErrorBoundary（2 个文件 +2 行每个），在 constants.ts 集中管理标签常量，不修改任何模块目录结构。

**Tech Stack:** TypeScript, React 19 (class component ErrorBoundary), Vite `import.meta.env.DEV`

---

### Task 1: 类型定义 — types.ts

**Files:**
- Create: `src/diagnostics/types.ts`

- [ ] **Step 1: 写入类型定义文件**

```ts
// 日志级别
export type LogLevel = 'debug' | 'info' | 'warn' | 'error';

// 模块标签格式：模块名/文件名
// 示例：'tasks/TaskForm' | 'tasks/taskService' | 'auth/LoginPage'
export type ModuleTag = string;

// 单条日志结构
export interface LogEntry {
  timestamp: number;        // Date.now()
  level: LogLevel;
  tag: ModuleTag;
  message: string;
  data?: Record<string, unknown>;
}

// 错误报告接口（预留外部监控接入，如 Sentry）
export interface IErrorReporter {
  report(error: Error, tag: ModuleTag, context?: Record<string, unknown>): void;
}

// Logger 实例接口（预留替换实现）
export interface ILogger {
  debug(message: string, data?: Record<string, unknown>): void;
  info(message: string, data?: Record<string, unknown>): void;
  warn(message: string, data?: Record<string, unknown>): void;
  error(message: string, error?: Error | unknown, data?: Record<string, unknown>): void;
  api(method: string, success: boolean, durationMs: number, data?: Record<string, unknown>): void;
}

// 诊断系统配置
export interface DiagConfig {
  level: LogLevel;
  target: 'console' | 'remote';
  errorReporter?: IErrorReporter;
}
```

- [ ] **Step 2: 验证 TypeScript 编译**

```bash
cd "E:\homework\开发\Claudecode\学生会交流平台" && npx tsc --noEmit src/diagnostics/types.ts
```
Expected: 无错误输出

---

### Task 2: 全局配置 — config.ts

**Files:**
- Create: `src/diagnostics/config.ts`

- [ ] **Step 1: 写入配置文件**

```ts
import type { DiagConfig } from './types';

export const diagConfig: DiagConfig = {
  // 开发模式输出所有级别（debug + info + warn + error），生产模式仅 info 及以上
  level: import.meta.env.DEV ? 'debug' : 'info',
  // 当前仅 console 输出；后期扩展 'remote' 可接入外部服务
  target: 'console',
  // errorReporter 在 errorReporter.ts 初始化时注入
};
```

- [ ] **Step 2: 验证 TypeScript 编译**

```bash
cd "E:\homework\开发\Claudecode\学生会交流平台" && npx tsc --noEmit src/diagnostics/config.ts
```
Expected: 无错误输出

---

### Task 3: 核心日志引擎 — logger.ts

**Files:**
- Create: `src/diagnostics/logger.ts`

- [ ] **Step 1: 写入 logger 引擎**

```ts
import { diagConfig } from './config';
import type { ILogger, LogLevel, ModuleTag } from './types';

const LEVEL_ORDER: Record<LogLevel, number> = {
  debug: 0, info: 1, warn: 2, error: 3,
};

/** 判断当前级别是否应该输出 */
function shouldLog(level: LogLevel): boolean {
  return LEVEL_ORDER[level] >= LEVEL_ORDER[diagConfig.level];
}

/** 格式化输出到 console */
function output(level: LogLevel, tag: ModuleTag, message: string, extra?: string): void {
  const prefix = `[${level.toUpperCase()}] [${tag}]`;
  const text = extra ? `${prefix} ${message} ${extra}` : `${prefix} ${message}`;

  switch (level) {
    case 'error':
      console.error(text);
      break;
    case 'warn':
      console.warn(text);
      break;
    case 'info':
      console.info(text);
      break;
    default:
      console.log(text);
      break;
  }
}

/** 创建绑定标签的 logger 实例 */
function createLogger(tag: ModuleTag): ILogger {
  return {
    debug(message, data) {
      if (shouldLog('debug')) {
        output('debug', tag, message, data ? JSON.stringify(data) : undefined);
      }
    },
    info(message, data) {
      if (shouldLog('info')) {
        output('info', tag, message, data ? JSON.stringify(data) : undefined);
      }
    },
    warn(message, data) {
      if (shouldLog('warn')) {
        output('warn', tag, message, data ? JSON.stringify(data) : undefined);
      }
    },
    error(message, err, data) {
      if (shouldLog('error')) {
        const errMsg = err instanceof Error ? err.message : String(err ?? '');
        const extra = data
          ? `{ error: "${errMsg}", ${JSON.stringify(data).slice(1)}`
          : `{ error: "${errMsg}" }`;
        output('error', tag, message, extra);
        // 同时调用 errorReporter（如果已配置）
        if (err instanceof Error) {
          diagConfig.errorReporter?.report(err, tag, data);
        }
      }
    },
    api(method, success, durationMs, data) {
      if (shouldLog('info')) {
        const status = success ? '完成' : '失败';
        const extra = data ? ` ${JSON.stringify(data)}` : '';
        output('info', tag, `${method} ${status} { duration: ${durationMs}ms }`, extra);
      }
    },
  };
}

/** 对外：通过标签获取 logger 实例 */
export const logger = {
  for: createLogger,
};
```

- [ ] **Step 2: 验证 TypeScript 编译**

```bash
cd "E:\homework\开发\Claudecode\学生会交流平台" && npx tsc --noEmit src/diagnostics/logger.ts
```
Expected: 无错误输出

---

### Task 4: API 调用追踪包装器 — serviceWrapper.ts

**Files:**
- Create: `src/diagnostics/serviceWrapper.ts`

- [ ] **Step 1: 写入 trace 包装函数**

```ts
import { logger } from './logger';
import type { ModuleTag } from './types';

/**
 * 包装单个 async 函数，自动追踪耗时和结果。
 * 不改原函数签名，返回值和异常照常传递。
 * 不做重试（重试属于 service 自身职责）。
 */
export function trace<T extends (...args: any[]) => Promise<any>>(
  tag: ModuleTag,
  methodName: string,
  fn: T,
): T {
  const log = logger.for(tag);
  return (async (...args: any[]) => {
    const start = performance.now();
    try {
      const result = await fn(...args);
      log.api(methodName, true, Math.round(performance.now() - start));
      return result;
    } catch (err) {
      log.api(methodName, false, Math.round(performance.now() - start));
      log.error(`${methodName} 执行失败`, err);
      throw err; // 原样抛出，不吞错误
    }
  }) as T;
}
```

- [ ] **Step 2: 验证 TypeScript 编译**

```bash
cd "E:\homework\开发\Claudecode\学生会交流平台" && npx tsc --noEmit src/diagnostics/serviceWrapper.ts
```
Expected: 无错误输出

---

### Task 5: 错误收集器 — errorReporter.ts

**Files:**
- Create: `src/diagnostics/errorReporter.ts`

- [ ] **Step 1: 写入错误收集器**

```ts
import type { IErrorReporter, ModuleTag } from './types';
import { diagConfig } from './config';

interface ErrorRecord {
  time: number;
  tag: ModuleTag;
  message: string;
}

// 最多保留最近 50 条，避免内存泄漏
const MAX_ERRORS = 50;
const errorLog: ErrorRecord[] = [];

/** 基础控制台错误收集实现 */
export const consoleErrorReporter: IErrorReporter = {
  report(error: Error, tag: ModuleTag, _context?: Record<string, unknown>) {
    errorLog.push({ time: Date.now(), tag, message: error.message });
    if (errorLog.length > MAX_ERRORS) {
      errorLog.shift();
    }
    // 预留：后期替换为 Sentry.captureException(error, { tags: { module: tag }, extra: context })
  },
};

/** 获取最近错误列表（供调试面板使用，预留） */
export function getRecentErrors(): ErrorRecord[] {
  return [...errorLog];
}

/** 初始化：注入 errorReporter 到全局配置 */
export function initErrorReporter(reporter?: IErrorReporter): void {
  diagConfig.errorReporter = reporter ?? consoleErrorReporter;
}
```

- [ ] **Step 2: 验证 TypeScript 编译**

```bash
cd "E:\homework\开发\Claudecode\学生会交流平台" && npx tsc --noEmit src/diagnostics/errorReporter.ts
```
Expected: 无错误输出

---

### Task 6: 统一导出入口 — index.ts

**Files:**
- Create: `src/diagnostics/index.ts`

- [ ] **Step 1: 写入导出入口**

```ts
// 诊断基础设施层统一入口
// 模块通过 `import { logger, trace } from '../../diagnostics'` 使用

export { logger } from './logger';
export { trace } from './serviceWrapper';
export { initErrorReporter, getRecentErrors } from './errorReporter';
export type { ILogger, IErrorReporter, LogLevel, ModuleTag, LogEntry, DiagConfig } from './types';
export { diagConfig } from './config';
```

- [ ] **Step 2: 验证全量 TypeScript 编译**

```bash
cd "E:\homework\开发\Claudecode\学生会交流平台" && npx tsc --noEmit src/diagnostics/index.ts
```
Expected: 无错误输出

---

### Task 7: 增强 ErrorBoundary

**Files:**
- Modify: `src/components/ErrorBoundary.tsx`

- [ ] **Step 1: 添加 import**

在 `src/components/ErrorBoundary.tsx` 顶部，`import { ReloadOutlined } from '@ant-design/icons';` 之后添加：

```tsx
import { logger } from '../diagnostics';
```

- [ ] **Step 2: 修改 componentDidCatch**

将 `src/components/ErrorBoundary.tsx` 第 25-27 行：

```tsx
  componentDidCatch(error: Error, info: { componentStack: string }) {
    console.error('[ErrorBoundary]', error, info.componentStack);
  }
```

替换为：

```tsx
  componentDidCatch(error: Error, info: { componentStack: string }) {
    logger.for('app/ErrorBoundary').error('全局渲染错误', error, {
      stack: info.componentStack?.slice(0, 500),
    });
  }
```

- [ ] **Step 2: 验证编译**

```bash
cd "E:\homework\开发\Claudecode\学生会交流平台" && npx tsc --noEmit
```
Expected: 无新增错误

---

### Task 8: 增强 ModuleErrorBoundary

**Files:**
- Modify: `src/components/ModuleErrorBoundary.tsx`

- [ ] **Step 1: 在文件顶部添加 import**

在现有 `import { ReloadOutlined } from '@ant-design/icons';` 之后添加：

```tsx
import { logger } from '../diagnostics';
```

- [ ] **Step 2: 修改 componentDidCatch**

将 `src/components/ModuleErrorBoundary.tsx` 第 25-27 行：

```tsx
  componentDidCatch(error: Error, info: { componentStack: string }) {
    console.error(`[ModuleErrorBoundary:${this.props.moduleName}]`, error, info.componentStack);
  }
```

替换为：

```tsx
  componentDidCatch(error: Error, info: { componentStack: string }) {
    logger.for(`${this.props.moduleName}/ModuleErrorBoundary`).error(
      '模块渲染错误',
      error,
      { module: this.props.moduleName },
    );
  }
```

- [ ] **Step 3: 验证编译**

```bash
cd "E:\homework\开发\Claudecode\学生会交流平台" && npx tsc --noEmit
```
Expected: 无新增错误

---

### Task 9: 添加 MODULE_TAGS 常量

**Files:**
- Modify: `src/utils/constants.ts`

- [ ] **Step 1: 在 constants.ts 末尾追加 MODULE_TAGS**

在文件末尾（第 84 行 `];` 之后）追加：

```ts
/** 诊断标签 — 模块/组件级，搜标签即定位到文件 */
export const MODULE_TAGS = {
  auth: {
    LoginPage: 'auth/LoginPage',
    authService: 'auth/authService',
  },
  tasks: {
    TaskListPage: 'tasks/TaskListPage',
    TaskDetail: 'tasks/TaskDetail',
    TaskForm: 'tasks/TaskForm',
    taskService: 'tasks/taskService',
  },
  notices: {
    NoticeList: 'notices/NoticeList',
    NoticeForm: 'notices/NoticeForm',
    noticeService: 'notices/noticeService',
  },
  school: {
    SchoolNoticeList: 'school/SchoolNoticeList',
    SchoolNoticeForm: 'school/SchoolNoticeForm',
    schoolService: 'school/schoolService',
  },
  profile: {
    ProfilePage: 'profile/ProfilePage',
    ChangePassword: 'profile/ChangePassword',
    TaskCalendar: 'profile/TaskCalendar',
    profileService: 'profile/profileService',
  },
  forum: {
    PostList: 'forum/PostList',
    PostDetail: 'forum/PostDetail',
    PostForm: 'forum/PostForm',
    forumService: 'forum/forumService',
  },
  admin: {
    MemberManage: 'admin/MemberManage',
    InviteCodeManage: 'admin/InviteCodeManage',
    adminService: 'admin/adminService',
  },
  tickets: {
    TicketList: 'tickets/TicketList',
    TicketForm: 'tickets/TicketForm',
    MyTickets: 'tickets/MyTickets',
    ticketService: 'tickets/ticketService',
  },
} as const;
```

- [ ] **Step 2: 验证编译**

```bash
cd "E:\homework\开发\Claudecode\学生会交流平台" && npx tsc --noEmit
```
Expected: 无新增错误

---

### Task 10: 初始化 errorReporter

**Files:**
- Modify: `src/main.tsx`

- [ ] **Step 1: 在应用入口初始化错误收集器**

在 `src/main.tsx` 的 `import App from './App';` 之后添加：

```tsx
import { initErrorReporter } from './diagnostics';
```

在 `createRoot(...)` 调用之前添加初始化调用。完整修改后的 main.tsx：

```tsx
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import { ConfigProvider } from 'antd';
import { HashRouter } from 'react-router-dom';
import zhCN from 'antd/locale/zh_CN';
import App from './App';
import theme from './theme';
import { initErrorReporter } from './diagnostics';

// 初始化诊断系统错误收集器
initErrorReporter();

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <ConfigProvider theme={theme} locale={zhCN}>
      <HashRouter>
        <App />
      </HashRouter>
    </ConfigProvider>
  </StrictMode>,
);
```

- [ ] **Step 2: 验证完整项目编译**

```bash
cd "E:\homework\开发\Claudecode\学生会交流平台" && npx tsc --noEmit
```
Expected: 无新增错误

---

### Task 11: Service 层集成示例 — taskService

**Files:**
- Modify: `src/modules/tasks/taskService.ts`

> 此任务以 taskService 为范本演示如何集成 trace 包装器。其余 7 个模块的 service 按同样模式执行（见 Task 12）。

- [ ] **Step 1: 添加 trace import 和 TAG 常量**

在 `src/modules/tasks/taskService.ts` 顶部 `import supabase from '../../supabaseClient';` 之后添加：

```ts
import { trace } from '../../diagnostics';

const TAG = 'tasks/taskService';
```

- [ ] **Step 2: 用 trace 包装所有导出函数**

将现有的 `export async function fetchTasks(...)` 改为 `export const fetchTasks = trace(TAG, 'fetchTasks', async (...))` 模式。

具体修改如下——

**fetchTasks**（替换第 32 行）:
```ts
export const fetchTasks = trace(TAG, 'fetchTasks', async (
  userId: string,
  department: string,
  role: string,
): Promise<Task[]> => {
  let query = supabase
    .from('tasks')
    .select('*, creator:created_by(name), assignee:assigned_to(name)')
    .order('created_at', { ascending: false });

  if (role === 'volunteer') {
    query = query.or(`assigned_to.eq.${userId},assigned_department.eq.${department}`);
  } else if (role === 'dept_head' || role === 'presidium') {
    query = query.eq('assigned_department', department);
  }

  const { data, error } = await query;

  if (error) {
    throw error;
  }

  return (data || []).map((t: Record<string, unknown>) => ({
    ...t,
    creator_name: (t.creator as { name: string } | null)?.name ?? '未知',
    assignee_name: (t.assignee as { name: string } | null)?.name ?? undefined,
  })) as unknown as Task[];
});
```

> 注意：原来 `console.error + return []` 的静默吞错模式改为 `throw error`，让 `trace()` 能正确记录失败。调用方（组件）需要处理错误。如果希望保持原有「返回空数组」的行为不变，可以改为在函数内部 catch 后 throw 或保持原样但不使用 trace。

**保持原有静默处理方案**（推荐——最小化行为变更）:

不对 taskService 做 trace 包装。改为保留原有 `console.error` + 返回 fallback 的模式，只替换 `console.error` 为结构化 logger：

```ts
import { logger } from '../../diagnostics';

const log = logger.for('tasks/taskService');
```

然后将函数内的 `console.error('fetchTasks error:', error);` 替换为 `log.error('fetchTasks 查询失败', error);`

- [ ] **Step 3: 验证编译**

```bash
cd "E:\homework\开发\Claudecode\学生会交流平台" && npx tsc --noEmit
```
Expected: 无新增错误

---

### Task 12: 验证与收尾

**Files:** 无新建，验证所有改动

- [ ] **Step 1: 全项目 TypeScript 编译检查**

```bash
cd "E:\homework\开发\Claudecode\学生会交流平台" && npx tsc --noEmit
```
Expected: 无错误

- [ ] **Step 2: Vite 构建验证**

```bash
cd "E:\homework\开发\Claudecode\学生会交流平台" && npx vite build
```
Expected: 构建成功，无错误

- [ ] **Step 3: 确认文件结构**

```bash
ls -la "E:\homework\开发\Claudecode\学生会交流平台\src\diagnostics\"
```
Expected: 6 个文件（index.ts, types.ts, config.ts, logger.ts, serviceWrapper.ts, errorReporter.ts）

- [ ] **Step 4: Commit**

```bash
cd "E:\homework\开发\Claudecode\学生会交流平台"
git add src/diagnostics/
git add src/components/ErrorBoundary.tsx
git add src/components/ModuleErrorBoundary.tsx
git add src/utils/constants.ts
git add src/main.tsx
git commit -m "feat: 添加诊断调试系统 — 结构化模块标签日志 + API 调用追踪"
```

---

### 附：其余模块 Service 集成指引

以下 7 个 service 文件已预留标签（在 MODULE_TAGS 中），按需使用 trace 或 logger：

| 文件 | TAG | 所属模块 |
|------|-----|:--:|
| `src/modules/auth/authService.ts` | `auth/authService` | ① 登录认证 |
| `src/modules/notices/noticeService.ts` | `notices/noticeService` | ③ 部门公告 |
| `src/modules/school/schoolService.ts` | `school/schoolService` | ④ 学校信息 |
| `src/modules/profile/profileService.ts` | `profile/profileService` | ⑤ 个人中心 |
| `src/modules/forum/forumService.ts` | `forum/forumService` | ⑥ 部门论坛 |
| `src/modules/admin/adminService.ts` | `admin/adminService` | ⑦ 权限管理 |
| `src/modules/tickets/ticketService.ts` | `tickets/ticketService` | ⑧ 抢票模块 |

每个 service 的集成模式与 Task 11 相同：
1. 添加 `import { logger } from '../../diagnostics';`
2. 创建 `const log = logger.for('<TAG>');`
3. 将函数内的 `console.error(...)` 替换为 `log.error('<函数名> 失败', error);`

---

### 影响范围总结

| 类型 | 数量 | 文件 |
|------|:--:|------|
| 新增 | 6 | `diagnostics/*.ts` |
| 修改（核心） | 4 | ErrorBoundary ×2, constants.ts, main.tsx |
| 修改（可选） | 8 | 各模块 service（按需逐步集成） |
| 不改 | — | 所有模块目录、路由、组件 UI、CSS、Supabase 配置 |
