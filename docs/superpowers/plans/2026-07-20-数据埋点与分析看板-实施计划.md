# 数据埋点与分析看板 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 零外部依赖建设使用数据埋点系统，采集 6 类关键事件，提供管理员专属数据看板

**Architecture:** Supabase `usage_events` 表存储事件 → `analytics.ts` 工具函数统一写入（fire-and-forget） → 6 个关键位置各加 1 行调用 → admin 模块新增数据看板 Tab（仅 president/teacher 可见）→ 4 并行查询汇总展示

**Tech Stack:** React 19 + TypeScript 6 + Ant Design 6 + Supabase + Vite 8

---

### Task 1: 数据库 — 追加 usage_events 表 DDL

**Files:**
- Modify: `supabase-migration.sql`（末尾追加）

- [ ] **Step 1: 在 supabase-migration.sql 末尾追加 DDL**

```sql
-- ============================================================
-- 第十三部分：使用事件埋点表（v3.2）
-- ============================================================

CREATE TABLE IF NOT EXISTS usage_events (
  id         BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  event_type TEXT NOT NULL,
  user_id    UUID REFERENCES auth.users(id) ON DELETE SET NULL,
  module     TEXT,
  action     TEXT,
  metadata   JSONB DEFAULT '{}',
  created_at TIMESTAMPTZ DEFAULT now()
);

CREATE INDEX IF NOT EXISTS idx_usage_type_time ON usage_events(event_type, created_at DESC);
CREATE INDEX IF NOT EXISTS idx_usage_user ON usage_events(user_id);
```

- [ ] **Step 2: 提醒用户手动执行迁移**

> ⚠️ 复制上述 SQL → Supabase Dashboard → SQL Editor → 执行。此步骤必须先完成，否则后续插入 usage_events 会失败。

- [ ] **Step 3: Commit**

```bash
git add supabase-migration.sql
git commit -m "db: 新增 usage_events 表 — 使用事件埋点（6 类事件 + JSONB 元数据）"
```

---

### Task 2: 埋点工具函数

**Files:**
- Create: `src/utils/analytics.ts`

- [ ] **Step 1: 创建 src/utils/analytics.ts**

```typescript
import supabase from '../supabaseClient';

export type EventType = 'page_view' | 'task_complete' | 'notice_read'
  | 'ticket_action' | 'error' | 'login';

interface TrackPayload {
  event_type: EventType;
  userId: string;
  module?: string;
  action?: string;
  metadata?: Record<string, unknown>;
}

/**
 * 记录使用事件
 * 
 * fire-and-forget：绝不阻塞用户操作、永不抛异常。
 * 调用方需要传入 userId（从 useAuth() 获取），避免每次额外查询 session。
 */
export function trackEvent(payload: TrackPayload): void {
  supabase
    .from('usage_events')
    .insert({
      event_type: payload.event_type,
      user_id: payload.userId,
      module: payload.module ?? null,
      action: payload.action ?? null,
      metadata: payload.metadata ?? {},
    })
    .then(({ error }) => {
      if (error) console.warn('[analytics]', error.message);
    })
    .catch(() => {});
}
```

- [ ] **Step 2: Commit**

```bash
git add src/utils/analytics.ts
git commit -m "feat: 新增 analytics.ts — fire-and-forget 埋点工具函数"
```

---

### Task 3: 埋点调用 — AppLayout 页面访问追踪

**Files:**
- Modify: `src/components/AppLayout.tsx`

- [ ] **Step 1: 在文件顶部新增 import**

在现有 import 区域追加：
```typescript
import { trackEvent } from '../utils/analytics';
```

- [ ] **Step 2: 在现有 useEffect 内追加页面访问埋点**

找到 `useEffect`（约第 64 行，`loadBadges` 的那个 effect），在其内部末尾追加：

```typescript
// 页面访问埋点（fire-and-forget）
trackEvent({
  event_type: 'page_view',
  userId: user.id,
  module: location.pathname.replace('/', '') || 'dashboard',
});
```

> ⚠️ **重要：** 需要把 `location.pathname` 加入该 useEffect 的依赖数组。当前依赖是 `[user.id]`，改为 `[user.id, location.pathname]`。

- [ ] **Step 3: 更新 useEffect 依赖数组**

将：
```typescript
}, [user.id]);
```
改为：
```typescript
}, [user.id, location.pathname]);
```

- [ ] **Step 4: Commit**

```bash
git add src/components/AppLayout.tsx
git commit -m "feat: AppLayout 添加页面访问埋点（location.pathname 变化触发）"
```

---

### Task 4: 埋点调用 — ErrorBoundary 错误捕获

**Files:**
- Modify: `src/components/ErrorBoundary.tsx`

- [ ] **Step 1: 新增 import**

```typescript
import { trackEvent } from '../utils/analytics';
```

- [ ] **Step 2: 在 componentDidCatch 中追加埋点（约第 26 行）**

在 `logger.for(...)` 调用之后追加：

```typescript
trackEvent({
  event_type: 'error',
  userId: 'unknown',
  module: 'error-boundary',
  action: 'render_error',
  metadata: {
    error: error.toString().slice(0, 500),
    componentStack: info.componentStack?.slice(0, 500) ?? '',
  },
});
```

- [ ] **Step 3: Commit**

```bash
git add src/components/ErrorBoundary.tsx
git commit -m "feat: ErrorBoundary 添加错误埋点（渲染错误自动记录）"
```

---

### Task 5: 埋点调用 — LoginPage 登录事件

**Files:**
- Modify: `src/modules/auth/LoginPage.tsx`

- [ ] **Step 1: 新增 import**

```typescript
import { trackEvent } from '../../utils/analytics';
```

- [ ] **Step 2: 在 onLoginSuccess 函数内部追加登录埋点**

找到 `onLoginSuccess` 函数定义（在 `LoginPage` 组件外部或内部顶层）。查看 `LoginPage` 接收 `onLoginSuccess` 作为 props，所以需要在函数体内回调 `onLoginSuccess(user)` 之前或之后追加。

在**第一次调用** `onLoginSuccess(user)` 之后追加（约第 92 行 — 学生注册成功）：

```typescript
trackEvent({
  event_type: 'login',
  userId: user.id,
  action: 'student_signup',
});
```

在 `message.success('登录成功')` 之后追加（约第 113 行 — 学生登录）：

```typescript
trackEvent({
  event_type: 'login',
  userId: user.id,
  action: 'student_login',
});
```

在 `onLoginSuccess(user)` 调用之后（约第 170 行 — 教师注册）：

```typescript
trackEvent({
  event_type: 'login',
  userId: user.id,
  action: 'teacher_signup',
});
```

在 `message.success('登录成功')` 之后（约第 191 行 — 教师登录）：

```typescript
trackEvent({
  event_type: 'login',
  userId: user.id,
  action: 'teacher_login',
});
```

> **简化：** 4 个地方都可以简化为统一在 `onLoginSuccess(user)` 调用后追加，不需要区分学生/教师。更好的方案——在 `onLoginSuccess` **函数体内**追加（如果有定义的话）。如果没有——在组件内每次调用 `onLoginSuccess(user)` 前追加一行。

> 💡 **最优方案：** 直接在 `LoginPage` 组件外部的 `onLoginSuccess` 回调位置（即 `App.tsx` 中传给 LoginPage 的回调函数）加埋点，不改 LoginPage。但这需要修改 App.tsx。为简洁起见，在 LoginPage 内部，每个 `onLoginSuccess(user)` 调用前加一行。

- [ ] **Step 3: 简化为两处**

学生登录成功处（第 112 行后）：
```typescript
message.success('登录成功');
trackEvent({ event_type: 'login', userId: user.id, action: 'student' });
onLoginSuccess(user);
```

教师登录成功处（第 190 行后）：
```typescript
message.success('登录成功');
trackEvent({ event_type: 'login', userId: user.id, action: 'teacher' });
onLoginSuccess(user);
```

> 注册成功时（第 91 行 + 第 169 行）也会调 `onLoginSuccess`，但注册 ≠ 登录事件，不需要埋。只埋真正的"输入密码后登录成功"。

- [ ] **Step 4: Commit**

```bash
git add src/modules/auth/LoginPage.tsx
git commit -m "feat: LoginPage 添加登录事件埋点（学生/教师登录成功触发）"
```

---

### Task 6: 埋点调用 — 任务提交 + 公告已读 + 抢票退票

**Files:**
- Modify: `src/modules/tasks/TaskListPage.tsx`（或 `TaskDetail.tsx`——任务提交在 TaskDetail 内）
- Modify: `src/modules/notices/NoticeList.tsx`
- Modify: `src/modules/tickets/TicketList.tsx`（抢票）
- Modify: `src/modules/tickets/MyTickets.tsx`（退票）

- [ ] **Step 1: TaskDetail.tsx — 任务提交埋点**

文件：`src/modules/tasks/TaskDetail.tsx`

新增 import（如果尚未导入）：
```typescript
import { trackEvent } from '../../utils/analytics';
```

在 `handleSubmit` 函数中，`result.success` 为 true 的分支里，`message.success('提交成功，等待审核')` 之后追加：

```typescript
trackEvent({
  event_type: 'task_complete',
  userId: user.id,
  module: 'tasks',
  action: 'submitted',
  metadata: { task_id: task.id, task_title: task.title.slice(0, 50) },
});
```

- [ ] **Step 2: NoticeList.tsx — 公告已读埋点**

文件：`src/modules/notices/NoticeList.tsx`

新增 import：
```typescript
import { trackEvent } from '../../utils/analytics';
```

在 `handleExpand` 函数中（约第 103 行 `markNoticeRead(noticeId, user.id)` 之后追加：

```typescript
trackEvent({
  event_type: 'notice_read',
  userId: user.id,
  module: 'notices',
  action: 'read',
  metadata: { notice_id: noticeId },
});
```

- [ ] **Step 3: TicketList.tsx — 抢票埋点**

文件：`src/modules/tickets/TicketList.tsx`

新增 import：
```typescript
import { trackEvent } from '../../utils/analytics';
```

在 `handleGrab` 函数中（约第 40 行 `message.success(result.message)` 之后追加：

```typescript
trackEvent({
  event_type: 'ticket_action',
  userId: user.id,
  module: 'tickets',
  action: 'grabbed',
  metadata: { ticket_id: ticket.id, ticket_title: ticket.title?.slice(0, 50) ?? '' },
});
```

- [ ] **Step 4: MyTickets.tsx — 退票埋点**

文件：`src/modules/tickets/MyTickets.tsx`

新增 import：
```typescript
import { trackEvent } from '../../utils/analytics';
```

在 `handleRefund` 函数中（约第 28 行 `message.success(result.message)` 之后追加：

```typescript
trackEvent({
  event_type: 'ticket_action',
  userId: user.id,
  module: 'tickets',
  action: 'refunded',
  metadata: { ticket_id: t.ticket_id },
});
```

- [ ] **Step 5: Commit**

```bash
git add src/modules/tasks/TaskDetail.tsx src/modules/notices/NoticeList.tsx src/modules/tickets/TicketList.tsx src/modules/tickets/MyTickets.tsx
git commit -m "feat: 任务提交/公告已读/抢票退票 添加埋点（各 +1 行 fire-and-forget）"
```

---

### Task 7: 数据看板 Service — fetchAnalyticsSummary()

**Files:**
- Modify: `src/modules/admin/adminService.ts`

- [ ] **Step 1: 在 adminService.ts 末尾追加查询函数**

```typescript
// ========== 数据看板：使用分析 ==========

export interface AnalyticsSummary {
  totalEvents: number;
  activeUsers7d: number;
  recent7d: number;
  topModule: string;
  pageRanking: { module: string; count: number }[];
  eventStats: { event_type: string; count: number }[];
  recentErrors: { created_at: string; module: string; action: string; metadata: unknown }[];
}

/** 获取数据看板汇总（仅管理员调用） */
export async function fetchAnalyticsSummary(): Promise<AnalyticsSummary> {
  const sevenDaysAgo = new Date(Date.now() - 7 * 24 * 60 * 60 * 1000).toISOString();

  const [
    totalRes,
    activeRes,
    recent7dRes,
    pageRankRes,
    eventStatsRes,
    errorRes,
  ] = await Promise.all([
    supabase.from('usage_events').select('id', { count: 'exact', head: true }),
    supabase.from('usage_events')
      .select('user_id', { count: 'exact', head: true })
      .gte('created_at', sevenDaysAgo)
      .not('user_id', 'is', null),
    supabase.from('usage_events')
      .select('id', { count: 'exact', head: true })
      .gte('created_at', sevenDaysAgo),
    supabase.from('usage_events')
      .select('module, id')
      .eq('event_type', 'page_view')
      .gte('created_at', sevenDaysAgo),
    supabase.from('usage_events')
      .select('event_type, id')
      .gte('created_at', sevenDaysAgo),
    supabase.from('usage_events')
      .select('created_at, module, action, metadata')
      .eq('event_type', 'error')
      .order('created_at', { ascending: false })
      .limit(10),
  ]);

  // 页面访问排名（客户端 GROUP BY）
  const moduleCount: Record<string, number> = {};
  for (const r of (pageRankRes.data || [])) {
    const m = (r as { module: string }).module || 'unknown';
    moduleCount[m] = (moduleCount[m] || 0) + 1;
  }
  const pageRanking = Object.entries(moduleCount)
    .sort((a, b) => b[1] - a[1])
    .slice(0, 20)
    .map(([module, count]) => ({ module, count }));

  const topModule = pageRanking.length > 0 ? pageRanking[0].module : '暂无数据';

  // 事件类型统计
  const typeCount: Record<string, number> = {};
  for (const r of (eventStatsRes.data || [])) {
    const t = (r as { event_type: string }).event_type;
    typeCount[t] = (typeCount[t] || 0) + 1;
  }
  const eventStats = Object.entries(typeCount)
    .sort((a, b) => b[1] - a[1])
    .map(([event_type, count]) => ({ event_type, count }));

  return {
    totalEvents: totalRes.count ?? 0,
    activeUsers7d: activeRes.count ?? 0,
    recent7d: recent7dRes.count ?? 0,
    topModule,
    pageRanking,
    eventStats,
    recentErrors: (errorRes.data || []) as AnalyticsSummary['recentErrors'],
  };
}
```

- [ ] **Step 2: Commit**

```bash
git add src/modules/admin/adminService.ts
git commit -m "feat: adminService 新增 fetchAnalyticsSummary() — 4 并行查询汇总使用数据"
```

---

### Task 8: 数据看板 UI — AnalyticsDashboard 页面

**Files:**
- Create: `src/modules/admin/AnalyticsDashboard.tsx`
- Create: `src/modules/admin/AnalyticsDashboard.module.css`
- Modify: `src/modules/admin/index.tsx`（追加导出）

- [ ] **Step 1: 创建 src/modules/admin/AnalyticsDashboard.tsx**

```typescript
import { useState, useEffect } from 'react';
import { Card, Statistic, Table, Spin, message, Row, Col, Empty } from 'antd';
import {
  EyeOutlined, UserOutlined, ThunderboltOutlined, TrophyOutlined,
} from '@ant-design/icons';
import { fetchAnalyticsSummary } from './adminService';
import type { AnalyticsSummary } from './adminService';
import styles from './AnalyticsDashboard.module.css';

const EVENT_LABELS: Record<string, string> = {
  page_view: '📄 页面访问',
  login: '👤 登录',
  task_complete: '✅ 任务完成',
  notice_read: '📢 公告已读',
  ticket_action: '🎫 抢票操作',
  error: '❌ 错误',
};

export default function AnalyticsDashboard() {
  const [data, setData] = useState<AnalyticsSummary | null>(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    fetchAnalyticsSummary()
      .then(setData)
      .catch(() => message.error('数据加载失败'))
      .finally(() => setLoading(false));
  }, []);

  if (loading) return <div style={{ textAlign: 'center', padding: 60 }}><Spin /></div>;
  if (!data) return <Empty description="暂无数据" />;

  const pageColumns = [
    { title: '模块', dataIndex: 'module', key: 'module', render: (m: string) => m || '未知' },
    { title: '访问次数', dataIndex: 'count', key: 'count', render: (c: number) => `${c} 次` },
  ];

  const errorColumns = [
    { title: '时间', dataIndex: 'created_at', key: 'created_at',
      render: (t: string) => new Date(t).toLocaleString('zh-CN'),
      width: 160 },
    { title: '模块', dataIndex: 'module', key: 'module', width: 120 },
    { title: '错误信息', dataIndex: 'metadata', key: 'metadata',
      render: (m: unknown) => {
        const meta = m as Record<string, string> | null;
        return meta?.error ?? meta?.message ?? JSON.stringify(m).slice(0, 100);
      },
      ellipsis: true },
  ];

  return (
    <div>
      <h2 style={{ fontSize: 20, fontWeight: 600, marginBottom: 20 }}>📈 数据看板</h2>

      <Row gutter={[16, 16]} style={{ marginBottom: 24 }}>
        <Col xs={12} md={6}>
          <Card><Statistic title="近7天事件数" value={data.recent7d} prefix={<ThunderboltOutlined />} /></Card>
        </Col>
        <Col xs={12} md={6}>
          <Card><Statistic title="近7天活跃用户" value={data.activeUsers7d} prefix={<UserOutlined />} suffix="人" /></Card>
        </Col>
        <Col xs={12} md={6}>
          <Card><Statistic title="总事件数" value={data.totalEvents} prefix={<EyeOutlined />} /></Card>
        </Col>
        <Col xs={12} md={6}>
          <Card><Statistic title="最热模块" value={data.topModule} prefix={<TrophyOutlined />} /></Card>
        </Col>
      </Row>

      <Row gutter={[16, 16]} style={{ marginBottom: 24 }}>
        <Col xs={24} md={12}>
          <Card title="📄 页面访问排名（近7天）" size="small">
            <Table
              dataSource={data.pageRanking}
              columns={pageColumns}
              rowKey="module"
              pagination={false}
              size="small"
              showHeader={false}
              locale={{ emptyText: '暂无数据' }}
            />
          </Card>
        </Col>
        <Col xs={24} md={12}>
          <Card title="📊 事件类型统计（近7天）" size="small">
            <Table
              dataSource={data.eventStats}
              columns={[
                { title: '类型', dataIndex: 'event_type', key: 'event_type',
                  render: (t: string) => EVENT_LABELS[t] ?? t },
                { title: '次数', dataIndex: 'count', key: 'count',
                  render: (c: number) => `${c} 次` },
              ]}
              rowKey="event_type"
              pagination={false}
              size="small"
              showHeader={false}
              locale={{ emptyText: '暂无数据' }}
            />
          </Card>
        </Col>
      </Row>

      <Card title="❌ 最近错误（最多10条）" size="small">
        <Table
          dataSource={data.recentErrors}
          columns={errorColumns}
          rowKey={(_, i) => String(i)}
          pagination={false}
          size="small"
          locale={{ emptyText: '🎉 暂无错误记录' }}
        />
      </Card>
    </div>
  );
}
```

- [ ] **Step 2: 创建 src/modules/admin/AnalyticsDashboard.module.css**

```css
/* AnalyticsDashboard — 数据看板
   样式由 antd Card/Statistic/Table 提供，无需额外自定义
   保留文件以备后续追加 */
```

- [ ] **Step 3: 修改 src/modules/admin/index.tsx — 追加导出**

当前内容：
```typescript
export { default as MemberManage } from './MemberManage';
```

改为：
```typescript
export { default as MemberManage } from './MemberManage';
export { default as AnalyticsDashboard } from './AnalyticsDashboard';
```

- [ ] **Step 4: Commit**

```bash
git add src/modules/admin/AnalyticsDashboard.tsx src/modules/admin/AnalyticsDashboard.module.css src/modules/admin/index.tsx
git commit -m "feat: 新增 AnalyticsDashboard 数据看板 — 4 统计卡片 + 排名 + 错误日志"
```

---

### Task 9: 集成 — MemberManage 加第三个 Tab

**Files:**
- Modify: `src/modules/admin/MemberManage.tsx`

- [ ] **Step 1: 新增 import**

```typescript
import AnalyticsDashboard from './AnalyticsDashboard';
```

- [ ] **Step 2: 在 Tabs 的 items 数组中追加第三个 Tab**

将：
```typescript
items={[
  { key: 'members', label: '👥 成员管理', children: memberContent },
  { key: 'overview', label: '📊 工作看板', children: <WorkOverview /> },
]}
```

改为：
```typescript
items={[
  { key: 'members', label: '👥 成员管理', children: memberContent },
  { key: 'overview', label: '📊 工作看板', children: <WorkOverview /> },
  ...(adminAccess ? [{ key: 'analytics', label: '📈 数据看板', children: <AnalyticsDashboard /> }] : []),
]}
```

> `adminAccess = isAdmin(user.role)` 已在文件中定义（约第 68 行），仅 president/teacher 可见。

- [ ] **Step 3: Commit**

```bash
git add src/modules/admin/MemberManage.tsx
git commit -m "feat: 权限管理新增「数据看板」Tab — 仅管理员可见"
```

---

### Task 10: Build 验证 + PWA 版本号更新

- [ ] **Step 1: 运行 TypeScript 编译 + Vite 构建**

```bash
npm run build
```

Expected: `tsc -b && vite build` 零错误。

常见 TS 关注点：
- `analytics.ts` 的 `supabase` 导入路径正确（`../../supabaseClient` → 确认在 `src/utils/` 下路径是 `../supabaseClient`）
- `AnalyticsDashboard.tsx` 中的 Table 列类型（antd Table columns 类型推断）
- `MemberManage.tsx` 中 `adminAccess` 变量已存在

- [ ] **Step 2: 如有 TS 错误，逐一修复**

- [ ] **Step 3: 更新 PWA 版本号**

`public/version.json`:
```json
{
  "version": "3.2.0",
  "date": "2026-07-20",
  "changelog": [
    "📊 数据埋点系统：6 类使用事件自动采集（页面/登录/任务/公告/抢票/错误）",
    "📈 管理员数据看板：近7天活跃、页面排名、事件统计、错误日志",
    "🔒 零外部依赖：数据留在 Supabase，fire-and-forget 零性能影响"
  ]
}
```

`public/sw.js`:
```javascript
var CACHE_VERSION = 'v3.2.0';
```

- [ ] **Step 4: 最终提交**

```bash
git add -A
git status  # 确认改动文件列表
git commit -m "feat: v3.2 — 数据埋点系统 + 管理员数据看板"
git push origin master
```

---

### Task 11: 更新审视报告 + README

- [ ] **Step 1: 更新审视报告更新记录**（统一路径 + 项目本地两份）

追加：
```
| 2026-07-20 | **v3.2 迭代：** 📊 数据埋点系统上线（6 类事件） + 📈 管理员数据看板 |
```

- [ ] **Step 2: 更新 README 更新日志**

追加版本条目。

---

## 实施顺序

```
Task 1 (DDL) → Task 2 (工具函数) → Task 3-6 (埋点调用，可并行) → Task 7 (Service) → Task 8-9 (UI + 集成) → Task 10 (验证 + 部署) → Task 11 (文档)
```

- Task 3-6 修改不同文件，可同时进行
- Task 7 必须在 Task 2 之后（依赖 `usage_events` 工具函数定义的字段名）
- Task 8 必须在 Task 7 之后（依赖 `fetchAnalyticsSummary` 函数）

## 次生影响检查

- [ ] `analytics.ts` 的 fire-and-forget 是否真的不抛异常？（`.catch(() => {})` 确保）
- [ ] AppLayout `useEffect` 依赖数组是否更新？（`location.pathname` 必须加入）
- [ ] LoginPage 埋点是否放在"登录成功"分支而非"注册成功"分支？
- [ ] Admin Tab 的 `adminAccess` 变量确认存在且正确？
- [ ] 所有埋点调用中 `userId` 来源为 `user.id`（来自 `useAuth()`），确认可用
