# 第一轮迭代：安全修复与体验增强 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 安全加固（硬编码密钥移入环境变量 + 默认密码随机生成）+ 任务搜索筛选 + 月报逾期修复 + 任务可编辑

**Architecture:** Service 层先行 → 组件层随后 → 集成验证。所有改动在现有模块内，不新建模块目录、不改路由。搜索筛选使用客户端过滤（不改 Service 查询），编辑功能复用 TaskForm 表单字段模式。

**Tech Stack:** React 19 + TypeScript 6 + Ant Design 6 + Supabase + Vite 8

**前置检查结果：**
- `resetMemberPassword` 调用方：`src/modules/admin/MemberManage.tsx:60`
  - 当前用法：`const ok = await resetMemberPassword(authId)` + `message.success('${name} 的密码已重置为 123456')`
  - 返回值变更后需同步修改该调用方

---

### Task 1: 环境变量配置

**Files:**
- Modify: `.env`
- Modify: `.env.example`

- [ ] **Step 1: 在 `.env` 末尾追加开发者密钥变量**

```env
VITE_SUPABASE_URL=https://bbyykrgitgawqwdgcxhp.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_bqf-AGBapk5C5pyAZHv3Hw_trEpyoQB
VITE_DEV_KEY=DEV2026PRESIDENT
```

- [ ] **Step 2: 在 `.env.example` 末尾追加（但值使用占位符演示格式）**

```env
VITE_SUPABASE_URL=https://你的项目ID.supabase.co
VITE_SUPABASE_ANON_KEY=你的anon_key
# 开发者入口密钥（可选，不配置则使用默认值 DEV2026PRESIDENT）
VITE_DEV_KEY=DEV2026PRESIDENT
```

---

### Task 2: 清理 LoginPage 硬编码密钥

**Files:**
- Modify: `src/modules/auth/LoginPage.tsx:267`

- [ ] **Step 1: 将硬编码密钥替换为环境变量引用**

将：
```typescript
const VALID_DEV_KEY = 'DEV2026PRESIDENT';
```

改为：
```typescript
const VALID_DEV_KEY = import.meta.env.VITE_DEV_KEY || 'DEV2026PRESIDENT';
```

> 保留 fallback 字符串，确保开发环境未配 `.env` 时不会崩溃。

- [ ] **Step 2: Commit**

```bash
git add .env .env.example src/modules/auth/LoginPage.tsx
git commit -m "fix: 硬编码开发者密钥移入环境变量 VITE_DEV_KEY"
```

---

### Task 3: 重置密码默认值改为随机生成

**Files:**
- Modify: `src/modules/admin/adminService.ts:124-132`
- Modify: `src/modules/admin/MemberManage.tsx:59-66`

- [ ] **Step 1: 修改 `resetMemberPassword` 函数签名和实现**

将：
```typescript
export async function resetMemberPassword(authId: string): Promise<boolean> {
  const { error } = await supabase.rpc('reset_user_password', {
    user_id: authId,
    new_password: '123456',
  });

  if (error) { log.error('resetMemberPassword 重置失败', error); return false; }
  return true;
}
```

改为：
```typescript
export async function resetMemberPassword(authId: string): Promise<string | false> {
  const newPassword = Math.random().toString(36).slice(-8);
  const { error } = await supabase.rpc('reset_user_password', {
    user_id: authId,
    new_password: newPassword,
  });

  if (error) { log.error('resetMemberPassword 重置失败', error); return false; }
  return newPassword;
}
```

- [ ] **Step 2: 修改调用方 `MemberManage.tsx` 的 `handleResetPassword`**

将：
```typescript
const handleResetPassword = async (authId: string, name: string) => {
  const ok = await resetMemberPassword(authId);
  if (ok) {
    message.success(`${name} 的密码已重置为 123456`);
  } else {
    message.error('重置失败，请检查 SQL 函数是否已创建');
  }
};
```

改为：
```typescript
const handleResetPassword = async (authId: string, name: string) => {
  const newPwd = await resetMemberPassword(authId);
  if (newPwd) {
    message.success(`${name} 的密码已重置为 ${newPwd}`);
  } else {
    message.error('重置失败，请检查 SQL 函数是否已创建');
  }
};
```

- [ ] **Step 3: Commit**

```bash
git add src/modules/admin/adminService.ts src/modules/admin/MemberManage.tsx
git commit -m "fix: 管理员重置密码默认值由 123456 改为随机 8 位字符串"
```

---

### Task 4: 修复月报逾期数硬编码为 0

**Files:**
- Modify: `src/modules/dashboard/dashboardService.ts:340-378`

- [ ] **Step 1: 在部门维度循环内追加逾期统计**

在 `for (const t of allTasksRes)` 循环体内，`deptStats[d].total++` 之后追加：

```typescript
if (t.status !== 'completed' && t.deadline && dayjs(t.deadline).isBefore(now)) {
  deptStats[d].overdue++;
}
```

完整上下文：
```typescript
for (const t of allTasksRes) {
  const d = t.assigned_department;
  if (!deptStats[d]) deptStats[d] = { completed: 0, total: 0, overdue: 0 };
  deptStats[d].total++;
  if (t.status === 'completed') deptStats[d].completed++;
  // 新增：统计逾期
  if (t.status !== 'completed' && t.deadline && dayjs(t.deadline).isBefore(now)) {
    deptStats[d].overdue++;
  }
}
```

- [ ] **Step 2: 修改总览逾期数为真实统计**

将第 378 行：
```typescript
const totalOverdue = 0; // 月报暂不统计逾期明细
```

改为：
```typescript
const totalOverdue = allTasksRes.filter(
  t => t.status !== 'completed' && t.deadline && dayjs(t.deadline).isBefore(now)
).length;
```

- [ ] **Step 3: Commit**

```bash
git add src/modules/dashboard/dashboardService.ts
git commit -m "fix: 月报逾期数由硬编码 0 改为真实查询（复用已有数据，零新增查询）"
```

---

### Task 5: 新增 updateTask() Service 函数

**Files:**
- Modify: `src/modules/tasks/taskService.ts`（在 `updateTaskStatus` 函数之后插入）

- [ ] **Step 1: 在 `updateTaskStatus` 函数（约第 196 行）之后插入新函数**

```typescript
/** 更新任务内容（仅发布者或管理员可调用） */
export async function updateTask(taskId: string, updates: {
  title?: string;
  content?: string;
  priority?: string;
  deadline?: string | null;
  assigned_to?: string | null;
  attachments?: Attachment[] | null;
}): Promise<boolean> {
  const { error } = await supabase
    .from('tasks')
    .update({ ...updates, updated_at: new Date().toISOString() })
    .eq('id', taskId);

  if (error) {
    log.error('updateTask 更新失败', error);
    return false;
  }
  return true;
}
```

- [ ] **Step 2: Commit**

```bash
git add src/modules/tasks/taskService.ts
git commit -m "feat: 新增 updateTask() 函数 — 支持编辑已发布任务的内容/优先级/截止时间/执行人/附件"
```

---

### Task 6: 任务搜索 + 筛选 UI

**Files:**
- Modify: `src/modules/tasks/TaskListPage.tsx`
- Modify: `src/modules/tasks/tasks.module.css`（新增搜索筛选栏样式 + 响应式断点）

- [ ] **Step 1: 在 `TaskListPage.tsx` 顶部新增 import**

已有 `Grid` 导入，确认 `Input`、`Select`、`Row`、`Col`、`Button` 已从 antd 导入。

在现有 import 中追加 `Input`、`Select`、`Row`、`Col`：
```typescript
import { Card, Tag, Tabs, Button, Spin, Modal, Empty, Segmented, message, Grid, Input, Select, Row, Col } from 'antd';
```

新增图标导入 `SearchOutlined`、`ReloadOutlined`：
```typescript
import { PlusOutlined, ClockCircleOutlined, TeamOutlined, UserOutlined, FileTextOutlined, SearchOutlined, ReloadOutlined } from '@ant-design/icons';
```

- [ ] **Step 2: 新增筛选状态变量**

在现有 `useState` 声明组后面追加（约第 33 行之后）：

```typescript
const [searchText, setSearchText] = useState('');
const [priorityFilter, setPriorityFilter] = useState('all');
const [deptFilter, setDeptFilter] = useState('all');
```

- [ ] **Step 3: 扩展 `filteredTasks` useMemo**

将现有：
```typescript
const filteredTasks = useMemo(() => {
  let result = filter === 'all' ? tasks : tasks.filter((t) => t.status === filter);
  if (memberFilter) {
    result = result.filter((t) =>
      t.assigned_to === memberFilter || t.created_by === memberFilter);
  }
  return result;
}, [filter, memberFilter, tasks]);
```

改为：
```typescript
const filteredTasks = useMemo(() => {
  let result = filter === 'all' ? tasks : tasks.filter((t) => t.status === filter);
  if (memberFilter) {
    result = result.filter((t) =>
      t.assigned_to === memberFilter || t.created_by === memberFilter);
  }
  // 搜索词：模糊匹配标题
  if (searchText.trim()) {
    const kw = searchText.trim().toLowerCase();
    result = result.filter((t) => t.title.toLowerCase().includes(kw));
  }
  // 优先级筛选
  if (priorityFilter !== 'all') {
    result = result.filter((t) => t.priority === priorityFilter);
  }
  // 部门筛选
  if (deptFilter !== 'all') {
    result = result.filter((t) => t.assigned_department === deptFilter);
  }
  return result;
}, [filter, memberFilter, tasks, searchText, priorityFilter, deptFilter]);
```

- [ ] **Step 4: 定义 `handleReset` 重置函数**

在 `useMemo` 之后追加：
```typescript
const handleReset = () => {
  setFilter('all');
  setSearchText('');
  setPriorityFilter('all');
  setDeptFilter('all');
};
```

- [ ] **Step 5: 在 Tabs 上方插入搜索筛选栏**

在 `<Tabs activeKey={filter} ...>` 之前插入：

```typescript
{/* 搜索筛选栏 */}
<Row gutter={[12, 8]} style={{ marginBottom: 16 }} align="middle">
  <Col xs={24} md={8}>
    <Input
      placeholder="搜索任务标题..."
      prefix={<SearchOutlined />}
      value={searchText}
      onChange={(e) => setSearchText(e.target.value)}
      allowClear
    />
  </Col>
  <Col xs={12} md={6}>
    <Select
      value={priorityFilter}
      onChange={setPriorityFilter}
      style={{ width: '100%' }}
      options={[
        { value: 'all', label: '全部优先级' },
        { value: 'urgent', label: '🔴 紧急' },
        { value: 'important', label: '🟠 重要' },
        { value: 'normal', label: '🔵 普通' },
      ]}
    />
  </Col>
  {hasMinRole(user.role, 'president') && (
    <Col xs={12} md={6}>
      <Select
        value={deptFilter}
        onChange={setDeptFilter}
        style={{ width: '100%' }}
        options={[
          { value: 'all', label: '全部部门' },
          ...Object.entries(DEPARTMENTS).map(([key, label]) => ({ value: key, label })),
        ]}
      />
    </Col>
  )}
  <Col xs={24} md={4}>
    <Button icon={<ReloadOutlined />} onClick={handleReset} block={!md}>
      重置
    </Button>
  </Col>
</Row>
```

需要在文件顶部导入 `DEPARTMENTS`：
```typescript
import { TASK_PRIORITIES, TASK_STATUSES, DEPARTMENTS } from '../../utils/constants';
```

- [ ] **Step 5 之前的验证点：** 检查 `DEPARTMENTS` 是否已在 import 中。当前 `import { TASK_PRIORITIES, TASK_STATUSES } from '../../utils/constants';` 需要追加 `DEPARTMENTS`。

- [ ] **Step 6: 在 `tasks.module.css` 末尾追加搜索筛选栏响应式样式（在现有 `@media (max-width: 768px)` 块内追加）**

在现有 `@media (max-width: 768px)` 块的最后的 `}` 之前追加：
```css
  .searchRow {
    margin-bottom: 8px;
  }
```

同时在文件末尾（`@media` 块之外）追加（可选——antd Row/Col 已自带响应式，主要是给重置按钮满宽用）。

- [ ] **Step 7: Commit**

```bash
git add src/modules/tasks/TaskListPage.tsx src/modules/tasks/tasks.module.css
git commit -m "feat: 任务列表新增搜索框 + 优先级/部门筛选 + 重置按钮（客户端过滤，响应式布局）"
```

---

### Task 7: 任务详情页新增编辑功能

**Files:**
- Modify: `src/modules/tasks/TaskDetail.tsx`

- [ ] **Step 1: 在 import 中追加所需组件和函数**

确认 `Modal`、`Form`、`DatePicker`、`Select` 已导入。追加 `EditOutlined` 图标：
```typescript
import { Descriptions, Tag, Button, Input, List, message, Popconfirm, Checkbox, Grid, Modal, Form, Select, DatePicker } from 'antd';
import { EditOutlined } from '@ant-design/icons';
```

追加 Service 函数导入：
```typescript
import {
  submitTask, fetchTaskSubmissions, reviewSubmission,
  updateHandoverNote, fetchLinkedNotices, updateTask,
} from './taskService';
```

确认常量导入已存在（无需改动）：
```typescript
// 已有：import { TASK_PRIORITIES, TASK_STATUSES, NOTICE_TYPES } from '../../utils/constants';
```

追加工具函数导入（`isAdmin`）：
```typescript
import { hasMinRole, formatDateTime, getDepartmentLabel, isAdmin } from '../../utils/helpers';
```

追加 FileUpload 导入（编辑附件用）：
```typescript
import FileUpload, { type Attachment } from '../../components/FileUpload';
```

- [ ] **Step 2: 新增编辑状态变量**

在现有 `useState` 组后面（约第 31 行之后）追加：

```typescript
const [editing, setEditing] = useState(false);
const [editForm] = Form.useForm();
const [editLoading, setEditLoading] = useState(false);
const [editAttachments, setEditAttachments] = useState<Attachment[]>(task.attachments ?? []);
```

- [ ] **Step 3: 定义编辑权限判断变量**

```typescript
const canEdit = (task.created_by === user.id || isAdmin(user.role)) && task.status !== 'completed';
```

- [ ] **Step 4: 编写 `handleEdit` 处理函数**

```typescript
const handleEdit = async () => {
  try {
    const values = await editForm.validateFields();
    setEditLoading(true);
    const ok = await updateTask(task.id, {
      title: values.title,
      content: values.content ?? '',
      priority: values.priority,
      deadline: values.deadline ? values.deadline.toISOString() : null,
      assigned_to: values.assigned_to || null,
      attachments: editAttachments,
    });
    setEditLoading(false);
    if (ok) {
      message.success('任务已更新');
      setEditing(false);
      onUpdate();
      onClose(); // 关闭详情 Modal，让父组件刷新列表
    } else {
      message.error('更新失败');
    }
  } catch {
    // 表单校验失败，不做任何操作
  }
};
```

- [ ] **Step 5: 编写打开编辑弹窗时预填表单的逻辑**

```typescript
const openEditModal = () => {
  editForm.setFieldsValue({
    title: task.title,
    content: task.content,
    priority: task.priority,
    deadline: task.deadline ? dayjs(task.deadline) : null,
    assigned_to: task.assigned_to || undefined,
  });
  setEditAttachments(task.attachments ?? []);
  setEditing(true);
};
```

需要追加 dayjs 导入：
```typescript
import dayjs from 'dayjs';
```

- [ ] **Step 6: 在 Descriptions 标题栏追加编辑按钮**

将 `<Descriptions title={task.title} ...>` 中的 `title` 改为包含按钮的 JSX：

```typescript
<Descriptions
  title={
    <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', flexWrap: 'wrap', gap: 8 }}>
      <span>{task.title}</span>
      {canEdit && (
        <Button icon={<EditOutlined />} type="link" onClick={openEditModal}>
          编辑
        </Button>
      )}
    </div>
  }
  bordered
  column={md ? 2 : 1}
  size="small"
  style={{ marginBottom: 20 }}
>
```

- [ ] **Step 7: 在组件 return 语句末尾（`</div>` 之前）追加编辑 Modal**

在 `</div>`（第 288 行）之前追加：

```typescript
{/* 编辑任务 Modal */}
<Modal
  title="编辑任务"
  open={editing}
  onCancel={() => setEditing(false)}
  onOk={handleEdit}
  confirmLoading={editLoading}
  okText="保存"
  cancelText="取消"
  width={md ? 600 : undefined}
  destroyOnClose
>
  <Form
    form={editForm}
    layout="vertical"
    initialValues={{
      priority: task.priority,
      title: task.title,
      content: task.content,
    }}
  >
    <Form.Item
      label="标题"
      name="title"
      rules={[{ required: true, message: '请输入任务标题' }]}
    >
      <Input maxLength={100} />
    </Form.Item>

    <Form.Item
      label="内容"
      name="content"
    >
      <Input.TextArea rows={4} maxLength={2000} />
    </Form.Item>

    <Form.Item
      label="优先级"
      name="priority"
      rules={[{ required: true, message: '请选择优先级' }]}
    >
      <Select
        options={Object.entries(TASK_PRIORITIES).map(([key, p]) => ({ value: key, label: p.label }))}
      />
    </Form.Item>

    <Form.Item label="截止时间" name="deadline">
      <DatePicker
        showTime
        style={{ width: '100%' }}
        placeholder="选择截止时间（可选）"
      />
    </Form.Item>

    <Form.Item label="执行人" name="assigned_to">
      <Input placeholder="输入成员 ID（留空=部门全体）" />
    </Form.Item>

    <Form.Item label="附件">
      <FileUpload value={editAttachments} onChange={setEditAttachments} />
    </Form.Item>
  </Form>
</Modal>
```

- [ ] **Step 8: Commit**

```bash
git add src/modules/tasks/TaskDetail.tsx
git commit -m "feat: 任务详情新增编辑功能 — 发布者/管理员可编辑标题/内容/优先级/截止时间/执行人/附件"
```

---

### Task 8: Build 验证

- [ ] **Step 1: 运行 TypeScript 编译 + Vite 构建**

```bash
npm run build
```

Expected: `tsc -b && vite build` 零错误。

- [ ] **Step 2: 如有 TS 类型错误，逐一修复**

常见关注点：
- `import.meta.env.VITE_DEV_KEY` 的类型：`vite/client` 已提供 `ImportMetaEnv` 接口，可能需要扩展 `.d.ts`
- `dayjs` 类型：已安装 `dayjs`，导入无误
- `DEPARTMENTS` 导入：确认已从 `constants.ts` 导入（check: `TaskListPage.tsx` 原先未导入 `DEPARTMENTS`）

**特别注意 TaskListPage.tsx：** 原先导入 `{ TASK_PRIORITIES, TASK_STATUSES }`，Task 6 中需要对 `DEPARTMENTS` 的引用，所以需要改为：
```typescript
import { TASK_PRIORITIES, TASK_STATUSES, DEPARTMENTS } from '../../utils/constants';
```

**特别注意 TaskDetail.tsx：** 原先导入 `{ TASK_PRIORITIES, TASK_STATUSES, NOTICE_TYPES }`，需要确认：
- 追加 `DEPARTMENTS`（如果 Select 执行人需要部门列表）
- 追加 `isAdmin` 从 `helpers.ts`
- 追加 `FileUpload`, `type Attachment` 从 `../../components/FileUpload`

---

### Task 9: 更新 PWA 版本号

**Files:**
- Modify: `public/version.json`
- Modify: `public/sw.js`

- [ ] **Step 1: 更新 `public/version.json`**

```json
{
  "version": "3.1.0",
  "date": "2026-07-20",
  "changelog": [
    "🔒 安全加固：开发者密钥移入环境变量，默认密码改为随机生成",
    "🔍 任务搜索筛选：支持标题搜索 + 优先级/部门筛选，移动端适配",
    "📊 修复月度报告逾期数始终显示 0 的 Bug",
    "✏️ 任务可编辑：发布者/管理员可修改已发布任务的内容/优先级/执行人等"
  ]
}
```

- [ ] **Step 2: 更新 `public/sw.js` 中的 `CACHE_VERSION`**

```javascript
var CACHE_VERSION = 'v3.1.0';
```

---

### Task 10: 最终提交与推送

- [ ] **Step 1: 检查所有改动**

```bash
git status
```

确认改动文件列表与预期一致。

- [ ] **Step 2: 提交并推送**

```bash
git add -A
git commit -m "feat: v3.1 — 安全加固 + 任务搜索筛选 + 月报逾期修复 + 任务可编辑"
git push origin master
```

---

## 次生影响检查清单

在执行过程中，每完成一个 Task 后确认：

- [ ] Task 2: LoginPage 开发者入口功能未受影响
- [ ] Task 3: `resetMemberPassword` 返回值变更 → `MemberManage.tsx` 已同步适配
- [ ] Task 4: 月报 API 返回值结构未变（仅 `totalOverdue` 从常量变为计算值，类型不变）
- [ ] Task 5: `updateTask` 是新增函数，不影响现有调用方
- [ ] Task 6: 搜索筛选栏在移动端（<768px）不溢出，看板模式下同样生效
- [ ] Task 7: 编辑 Modal 在移动端宽度正常，不可编辑部门字段
- [ ] Task 8: `npm run build` 零错误

## 实施顺序

```
Task 1 → Task 2 → Task 3 → Task 4 → Task 5 → Task 6 → Task 7 → Task 8 → Task 9 → Task 10
         ↑────── Service 层 ──────↑  ↑──── 组件层 ────↑  ↑验证↑  ↑发布↑
```

Service 层可并行的任务：Task 2, 3, 4, 5 互不依赖（改不同文件），可同时进行。
组件层必须串行：Task 6 和 Task 7 无依赖关系，可同时进行。
